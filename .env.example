# Core app
NODE_ENV=development
PORT=4000
BACKEND_PORT=4000
FRONTEND_PORT=3000
POSTGRES_PORT=5432
REDIS_PORT=6379

# Backend persistence
DB_PASSWORD=sri_sudha_dev_pass
JWT_SECRET=sri-sudha-dev-secret

# Translation providers
# Preferred: Torouter chat completions provider
TOROUTER_API_URL=https://portal.torouter.ai/api/v1/chat/completions
TOROUTER_API_KEY=
TOROUTER_MODEL=gpt-4o-mini

# Optional fallback: Python Google Translate v3 microservice
TRANSLATE_SERVICE_URL=http://localhost:5001

# Optional fallback: Google Translate REST API key
GOOGLE_TRANSLATE_KEY=
# Frontend Environment Variables
VITE_API_BASE_URL=http://localhost:4000/api

# Backend Environment Variables
NODE_ENV=development
DB_MODE=memory
DATABASE_URL=
REDIS_URL=
JWT_SECRET=sri-sudha-dev-secret-key
JWT_EXPIRES_IN=24h
REFRESH_TOKEN_EXPIRES_IN=7d
PORT=4000

# Database
DB_USER=sri_sudha
DB_PASSWORD=sri_sudha_dev_pass
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sri_sudha

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Analytics (optional)
ANALYTICS_URL=https://analytics.example.com
ANALYTICS_API_KEY=your-api-key

# Security
CORS_ORIGIN=http://localhost:3000
CORS_CREDENTIALS=true

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# Email (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Sentry (optional for error tracking)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# Snyk (optional for security scanning)
SNYK_TOKEN=your-snyk-token
